---
type: concept
aliases: [RAG, 検索拡張生成, dense retrieval, DPR, retrieval, GraphRAG, Graph RAG, QFS, query-focused summarization, クエリ焦点型要約, sensemaking, センスメイキング, community detection, コミュニティ検出, Leiden]
tags: [retrieval-augmented-generation, llm-agents, agent-memory]
related:
  - "[[tool-use-and-function-calling]]"
  - "[[agent-memory]]"
  - "[[reasoning-and-planning]]"
  - "[[context-engineering]]"
  - "[[agent-evaluation]]"
summaries:
  - "[[summaries/2023-swe-bench]]"
  - "[[summaries/2024-graphrag]]"
  - "[[summaries/2025-effective-context-engineering]]"
  - "[[summaries/2020-rag]]"
  - "[[summaries/2025-multi-agent-research-system]]"
  - "[[summaries/2023-memgpt]]"
  - "[[summaries/2025-a-mem]]"
  - "[[summaries/2026-deepseek-v4]]"
  - "[[summaries/2025-llm-reasoning-to-agents]]"
updated: 2026-08-02
---

# Retrieval-Augmented Generation（検索拡張生成）

**外部知識を検索してモデルに与え、それを根拠に生成させる**手法の総称。LLM（Large Language Model, 大規模言語モデル）の 3 つの構造的弱点——知識を更新できない・出所を示せない・幻覚する——への最も広く使われる対処であり、エージェントにとっては「世界の最新の事実にどう接続するか」の基本部品である。

RAG には歴史的に **2 つの実装層**があり、混同しやすいので分けて理解する:

1. **訓練時に組み込む型**（原典型）— 検索器と生成器を単一の確率モデルとして end-to-end に訓練する。[[summaries/2020-rag]]（2020, 命名の原典）がこの型。
2. **推論時に注入する型**（現代の主流）— 既製の LLM のプロンプトに検索結果を入れるだけ。長コンテキストの LLM と in-context 学習の発達で、ファインチューニングなしのこの型が実務標準になった（chunking＝文書分割 → embedding＝ベクトル化 → 検索 → reranking＝再ランキング → プロンプト注入、のパイプライン。専用原典は未 ingest のため概説）。

層は違っても、原典が確立した設計原則——**知識はパラメータでなく索引に置く**——は共通している。

ただしこの 2 層はどちらも「**答えがテキストのどこかに局所的に書いてある**」ことを前提にしている。この前提が成り立たない質問のクラスがあり、そこを扱うのが 3 番目の軸である → 下記の [[summaries/2024-graphrag]]（GraphRAG）。

## 代表手法

### RAG（Lewis et al., 2020）— 命名の原典

[[summaries/2020-rag]]。パラメトリック記憶（BART の重み）と非パラメトリック記憶（Wikipedia 2100 万チャンクの密ベクトル索引）を、**検索文書を潜在変数として周辺化**する形で結合し、どの文書を使うべきかの教師なしで検索まで学習した。今日まで通用する 3 つの実証:

- **幻覚の減少**: 人手評価で「より事実的」42.7% vs BART 7.1%。パラメトリックと非パラメトリックの協働（検索が引き金を引き、モデル内部の知識が補完する）の分析も残した。
- **index hot-swapping**: 索引を差し替えるだけで再訓練ゼロの知識更新（2016/2018 年版 Wikipedia で世界指導者の正答が入れ替わる実証）。「ドキュメントを差し替えれば済む」という現代の RAG 運用の原型。
- **retrieval collapse**: 知識をあまり要さないタスクでは検索器が入力を無視して固定文書に崩壊し、生成器も文書を無視するようになる——RAG の適用境界の最初の記録。

### DPR / REALM — 検索側の土台

**DPR**（Dense Passage Retriever）は BERT の bi-encoder（クエリ用・文書用）で埋め込み、最大内積探索（MIPS, FAISS で近似）で文書を引く密検索器で、原典 RAG の検索部品。**REALM** は検索つき事前学習の先行（抽出型 QA まで）。いずれも概説（専用原典は未 ingest）。なお原典のアブレーションでは、**エンティティ中心のタスクでは古典的な BM25（単語の重なり）が密検索に勝つ**ことも示されており、「密検索一択にしない」は当時からの教訓である。

### GraphRAG（Edge et al., Microsoft, 2024）— 検索では答えられない質問へ

[[summaries/2024-graphrag]]。上の 2 手法が「**どう検索するか**」の改善なのに対し、GraphRAG は「**検索では原理的に答えられない質問がある**」という診断から始まる。

「このデータセットの主要なテーマは何か」「これらの報告書に共通する懸念は何か」——こうした**コーパス全体に向けられたグローバルな質問**に対して、意味的に近いチャンクを top-K 個引くことには意味がない。答えは特定のチャンクの中に書かれていないからである。これは検索タスクではなく**クエリ焦点型要約**（Query-Focused Summarization, QFS。与えられたクエリの観点からコーパス全体を要約するタスク）であり、人間の側の営みとしては**センスメイキング**（つながりを理解しようとする継続的な努力）にあたる。従来の QFS 手法は RAG が扱う規模へスケールしなかったので、両者の隙間が空いていた。

**解法は、検索を改善するのではなく索引の作り方を変えることである**。

1. LLM に各チャンクからエンティティ・関係・（任意で）主張を抽出させ、**エンティティの知識グラフ**を作る。
2. そのグラフを **Leiden アルゴリズム**でコミュニティ検出し、階層的に分割する。この分割は**相互排他かつ全体網羅（MECE）**なので、各コミュニティの要約を集めれば**コーパス全体の完全なカバレッジ**になる。
3. 各コミュニティの**要約を事前生成**しておく（索引化時）。
4. 質問が来たら、全コミュニティ要約に**並列に答えさせ**（map。同時に「この答えは有用か」を 0〜100 で自己採点させ、0 は捨てる）、有用度順に詰めて**最終回答を合成する**（reduce）。

**「検索して絞る」のではなく「全部に問い合わせて畳む」**——この転換が本手法の骨格である。だから「検索の質が生成の上限」（下記の設計論点）という制約自体が外れる。

通常の知識グラフとの違いも重要で、**ノードが持つのは正規化された知識トリプル（主語・述語・目的語）ではなく豊かな記述テキスト**である。読み手が LLM なので、記号的な一貫性より自然言語の記述のほうが扱いやすい、という割り切り。同じ理由でエンティティ解決（表記ゆれの統合）も明示的には行わず、「コミュニティ検出が吸収する」ことに賭けている。

**実測**（100 万トークン級のコーパス 2 つ、gpt-4-turbo）。素朴な RAG に対して網羅性の勝率 **72〜83%**、多様性 **62〜82%**。ただし比較対象として置かれた**グラフなしの手法——原文にそのまま map-reduce をかける TS——も競争力があり**、Graph RAG の TS に対する優位は 52〜64% と大きくない。**この論文の重心は「グラフが強い」ではなく「グローバルな map-reduce が素朴な RAG より強い」ほうにあり、グラフはそれを安くする手段**と読むのが正確である。実際、ルート水準のコミュニティ要約（C0）はクエリあたりの文脈トークンが原文 map-reduce の **2.3〜2.6%**（9〜43 倍安い）で、素朴な RAG に対する優位（網羅性 72%）は保つ。

**トレードオフはグラフ索引の構築コストで、これはクエリ時のトークン表に含まれていない**。チャンクごとの抽出 LLM 呼び出し＋要素要約＋全階層のコミュニティ要約が先に必要になる。著者ら自身が「**データセットあたりの生涯クエリ数の見込み**」を判断材料に挙げており、単発の分析なら TS のほうが安い。**償却の問題**として設計する。

## 設計論点

- **その質問はローカルか、グローバルか**: 最初に立てるべき分類。「A 社の 2023 年の売上は」は**ローカル**——答えが特定のチャンクに書かれているので検索で解ける。「これらの報告書に共通する懸念は」は**グローバル**——答えはどこにも書かれておらず、コーパス全体を畳まないと出てこない。[[summaries/2024-graphrag]] の最大の実務的貢献はこの線を引いたことにあり、**同じ索引で両方を捌こうとしない**のが設計上の含意になる。以下の設計論点の大半はローカル側の話である。
- **検索指標の改善は、成果の改善を意味しない**: 直感に反する重要な反例が [[summaries/2023-swe-bench]]（2023）にある。BM25 の最大文脈を広げると**オラクルのファイルに対する recall は 29.58 → 44.41 → 51.06 と改善する**のに、**タスクの解決率は 1.96 → 1.87 → 1.22 と下がる**。同論文は逆向きの実験も行っており、**オラクルのファイルのうち実際に編集された行の ±15 行以外を畳む**と Claude 2 が 4.8 → 5.9 へ**上がる**。**recall を最適化目標に据えると下流が壊れうる**——検索器の評価指標と、それを使う系の成果は別々に測らねばならない。なお同論文が密検索でなく BM25 を選んだ理由も実務的で、**キーとクエリが極端に長く、しかも自然言語のクエリでコード文書を引く**という設定では密検索が不適だと判断している（→ 下記「エンティティ中心では BM25 が密検索に勝つ」とも通じる）。
- **検索の質が生成の上限**: 悪い検索結果は推論全体を脱線させる。[[summaries/2022-react]] でも検索失敗がエラーの 23% を占めた——層が違っても（訓練型でもループ型でも）この制約は同じ。
- **単発検索か、反復検索か**: 原典 RAG は 1 回の検索で生成する。「検索→読んで→足りなければ再検索」という反復は、[[summaries/2022-react]] がエージェントのループ内ツール呼び出しとして解いた（→ [[tool-use-and-function-calling]]）。「内部知識で足りるか、検索すべきか」のルーティング（ReAct ⇄ CoT-SC のフォールバック）も同系列の論点。本番プロダクトの側でもこの対比は明示されている: Anthropic Research（[[summaries/2025-multi-agent-research-system]]）は自らのアーキテクチャを「**静的検索**（類似チャンクを一度取って生成する従来型 RAG）に対する**動的な多段検索**——見つけたものに適応しながら検索を重ね、結果を分析して回答を組み立てる——」と位置づけ、さらに検索を並列サブエージェント群に分散した。エージェント時代の RAG は「1 回の検索」から「検索戦略の運用」へと重心が移っている。この発展形を横断サーベイ（[[summaries/2025-llm-reasoning-to-agents]], 2025）は **Agentic RAG** として体系化する: 従来の RAG に自律エージェントの設計パターン（planning・reflection・ツール利用・マルチエージェント協調）を埋め込み、検索と反復的洗練を動的にオーケストレートする枠組みで、同サーベイの比較表（Table VI）は LLM 事前学習／事後訓練／RAG／AI エージェント／Agentic RAG の 5 段階を自律性・学習・信頼性で対比し、「**RAG の事実接地（静的だが正確）× AI エージェントの動的適応性**」の掛け合わせと位置づける。また同サーベイ §V-E の **ReSearch** は、検索を「別個の検索ステップ」でなく**推論連鎖に織り込まれたトークン化された行動**として扱い、最終答えの精度と think/search/result 形式の遵守を報酬に強化学習で訓練する——注釈付き推論トレースなしでマルチホップ QA の強い性能を得る、agentic retrieval を訓練で作り込む方向である → [[reinforcement-learning-from-human-feedback]]。
- **agentic search vs RAG の実測**: この対比を**同一モデル・同一タスク群で定量比較**した最初期の公開データが DeepSeek-V4（[[summaries/2026-deepseek-v4]]）の本番評価である。同社チャットボットは non-think モードで RAG（検索結果の一括注入）、thinking モードで agentic search（モデルがクエリごとに検索・取得ツールを反復呼び出し）を使い分けており、ペアワイズ評価では **agentic search が全難度帯で優位、差は複雑なタスクほど拡大**した。注目すべきはコスト側で、agentic search の平均はツール呼び出し 16.2 回（**大半は並列**）・prefill 13,649 トークン・出力 1,526 トークン——RAG（prefill 10,453・出力 1,308）に対して**わずかな増分**に収まる。「反復検索は高くつく」という直感への反証であり、並列ツール呼び出しと KV cache 再利用が効く環境では、agentic search をデフォルトにしない理由はコストではなくレイテンシ要件になる。
- **top-K の上限を破る — ページング反復**: 単発検索には「retriever が top-K に正解を入れられなければ、生成側は決して正解を見られない」という構造的上限がある。[[summaries/2023-memgpt]]（MemGPT, 2023）はこれを実証つきで突いた: 文書 QA で固定コンテキストのベースラインは retriever の性能で頭打ちになるのに対し、MemGPT は archival storage（ベクトル検索つき文書 DB）への**ページネーション付き検索を function call で何度でも発行**でき、正解がランキングの深い位置にあっても原理的には到達できる——コンテキスト長を増やしても性能が変わらない平坦な曲線がその帰結である。ただし同論文は、モデルが**データベースを使い尽くす前に検索を打ち切ってしまう**ことも観察しており、「どこまで粘って引くか」という読み出し側の挙動が新しいボトルネックになることを示した。
- **agentic RAG と agentic memory の境界**: 「いつ・何を検索するかをモデルが決める」検索側の自律化（agentic RAG。Self-RAG や FLARE の系譜——概説）に対し、[[summaries/2025-a-mem]]（A-Mem, 2025）は**保存・組織化の側の自律化**を対置した: 検索がどれだけ賢くても、索引そのものが静的なら知識ベースは育たない。A-Mem は保存時に LLM が意味づけ（キーワード・タグ・文脈説明）を済ませ、記憶間のリンクを判断し、新しい記憶の到着で既存記憶を書き換える（memory evolution）。**「検索の agency」と「索引の agency」は独立した設計軸**であり、後者は本ページの範囲を超えて [[agent-memory]] の領域に入る——両ページの境界線がここにある。
- **事前に埋め込むか、実行時に引くか（just-in-time）**: RAG の標準形は「クエリを埋め込んで**推論の前に**関連文書を集め、コンテキストへ注入する」——[[summaries/2025-effective-context-engineering]]（Anthropic, 2025）はこれを **embedding-based pre-inference time retrieval** と名指しし、エージェント的アプローチではこれを **just-in-time（JIT）** 戦略で補強する流れがあると整理した。JIT では関連データを事前処理せず、**軽量な識別子（ファイルパス・保存済みクエリ・Web リンク）だけを保持して、実行時にツールで動的に読み込む**。Claude Code が大規模データベースを、的を絞ったクエリと `head` / `tail` で**全体をコンテキストに載せずに**分析するのがその例。これは前項の agentic search と重なるが、力点が違う——agentic search が「**いつ検索するかをモデルが決める**」話なのに対し、JIT は「**そもそも索引を事前に作らず、参照だけ持って必要時に実体を引く**」という**格納側**の設計である。副産物として、**参照のメタデータ自体が信号になる**（`tests/test_utils.py` と `src/core_logic/test_utils.py` は目的が違う。階層・命名規約・タイムスタンプが手がかりを与える）ことと、**progressive disclosure（段階的開示）**——探索を通じて関連コンテキストを層ごとに組み立てる——が挙げられている。
  - トレードオフは明快で、**実行時の探索は事前計算された取得より遅い**。加えて、ツールとヒューリスティクスの設計が悪いとエージェントは行き止まりを追ってコンテキストを浪費する。実務解は**ハイブリッド**で、境界は「内容の動的さ」で引く——Claude Code は `CLAUDE.md` を事前投入しつつ `glob` / `grep` で JIT に引くことで**陳腐化した索引や複雑な構文木の問題を回避**しており、逆に法務・財務のように内容が安定した領域は事前取得寄りが適するとされる。「索引を作るコスト・陳腐化するリスク」と「毎回探索する遅さ」のどちらを払うかの選択と読める → [[context-engineering]]。
- **知識の置き場所の経済学**: 原典は 626M パラメータで T5-11B（closed-book）を圧倒した。知識はパラメータに焼き込むより索引に置く方が安く、更新可能で、監査できる——[[agent-memory]] の外部記憶設計へ通じる原則。
- **文書数と合成の粒度**: 検索文書を増やせば単調に良くなるとは限らない（RAG-Token は 10 でピーク）。複数文書の内容を 1 つの答えへ合成する必要があるかどうかで、設計（原典なら Token/Sequence、現代ならプロンプトへの並べ方 → [[context-engineering]]）が変わる。
- **collapse の監視**: 検索結果が入力によらず似通ってきたら検索が死んでいるサイン。RAG パイプラインのヘルスチェック項目（→ [[agent-evaluation]]）。
- **索引のバイアス**: 検索で接地しても、索引が誤っていれば誤りに接地する。知識源の品質管理は RAG の外側で必要。
- **索引を作るコストは、生涯クエリ数で割って考える**: 前処理の重い索引（GraphRAG のグラフ索引、階層的な要約、埋め込みの再計算）は、同じコーパスに何度問うかで採算が決まる。1 回しか問わないなら索引を作らず原文を直接畳むほうが安い。逆に反復的なセンスメイキングでは、クエリ単価が桁で効いてくる。**この軸は前項の just-in-time（索引を作らず参照だけ持つ）とも連続していて**、「事前索引の重さ」を 0 →埋め込み → グラフ索引と並べたスペクトルの上で選ぶ問題になる。
- **コンテキストは大きいほど良いとは限らない（実測で選ぶ）**: [[summaries/2024-graphrag]] は 8k / 16k / 32k / 64k を比較し、**最小の 8k がすべての比較で網羅性において一様に優れた**（平均勝率 58.1%）——gpt-4-turbo が 128k を扱えるにもかかわらず、である。著者らはこれを lost in the middle に帰している。「使えるだけ詰める」を既定にしない、という [[context-engineering]] の主題を 2024 年前半の実測で裏づけた例。

## 関連ページ

- [[tool-use-and-function-calling]] — エージェントのループ内検索としての RAG
- [[agent-memory]] — 非パラメトリック記憶・hot-swap の発展先
- [[reasoning-and-planning]] — 内部知識と外部検索のルーティング
- [[context-engineering]] — 検索結果をコンテキストへどう積むか
- [[summaries/2020-rag]] — 本ページの主要な根拠原典
- [[summaries/2024-graphrag]] — グローバルな質問へのコミュニティ要約 map-reduce
- [[summaries/2026-deepseek-v4]] — agentic search vs RAG の同一モデル実測
