---
type: summary
source_path: raw/papers/SWE-agent- Agent-Computer Interfaces Enable Automated Software Engineering.pdf
source_kind: paper
title: "SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering"
authors: [John Yang, Carlos E. Jimenez, Alexander Wettig, Kilian Lieret, Shunyu Yao, Karthik Narasimhan, Ofir Press]
year: 2024
venue: NeurIPS 2024（arXiv:2405.15793）
ingested: 2026-08-02
tags: [coding-agents, tool-use-and-function-calling, agent-evaluation, agent-computer-interface, swe-bench, context-engineering]
translation: "[[translations/2024-swe-agent]]"
---

# SWE-agent — 人間向けに良い UI が、エージェントには有害でありうる

> 原典: [[translations/2024-swe-agent]] ・ `raw/papers/SWE-agent- Agent-Computer Interfaces Enable Automated Software Engineering.pdf`
> 著者・年・会議: John Yang, Carlos E. Jimenez ほか（Princeton Language and Intelligence, Princeton University）/ NeurIPS 2024 / arXiv:2405.15793

## 一言まとめ

**LM（Large Language Model, 大規模言語モデル）は「独自の必要と能力を持つ新しいカテゴリのエンドユーザー」である**と措定し、**モデルの重みを一切変えずにインターフェースだけを設計し直す**ことで SWE-bench の解決率を 3.8% → **12.47%** へ引き上げた論文。この「エージェント–コンピュータインターフェース（**ACI**, agent-computer interface）」という概念そのものが、本論文の主たる貢献である。

## 背景と問題意識 — なぜ「インターフェース」が変数になるのか

エージェントに実世界のソフトウェア工学をやらせるとき、素朴な出発点は **Linux シェルをそのまま渡すこと**である。すでにあるし、何でもできる。

ところが実際にやってみると LM は苦戦する。著者らの診断が具体的である——**シェルはファイルの小さな断片を編集する単純なコマンドを提供していないし、ユーザーが無効な編集をしても何のフィードバックも返さない**。`sed` で複数行を書き換えるには引数を繊細に調整した複数回の呼び出しが要り、しかもそれは「黙った」操作なので、編集が効いたか確認するには余計なコマンドが要る。

ここから著者らは、**人間と LM の違いがインターフェース設計の指針を分ける**と論じる。

- 人間は不要な情報を柔軟に無視できるが、**LM にとってはすべての内容がメモリと計算において固定のコストを持ち、気を散らす文脈は性能を害する**（→ [[context-engineering]]）。
- 人間には上下キーの連打が直観的だが、**LM にとってそれは冗長で高くつく**。
- 一方で、IDE が人間に提供する機能（構文検査・ナビゲーション）の多くは、**適切な形で提示されれば LM にも有用**である。

つまり既存のインターフェースは**ソフトウェア（API）か人間（UI）のどちらかを念頭に設計されており、第 3 のエンドユーザーが想定されていない**。その第 3 のインターフェースが **ACI** である。

<figure>

![](../../raw/assets/2024-swe-agent/fig02-aci-vs-ui.png)

<figcaption>図1（再掲, 原典の Figure 2）: IDE（VSCode, PyCharm）が人間をコンピュータのタスクで効率的にするのと同様に、ACI の設計は LM エージェントをデジタルな仕事で効果的にする適切なインターフェースを作ることを狙う。左が LM エージェント → ACI（File Viewer / File Editor / Code Search）→ Computer、右が Human → UI → Computer。</figcaption>
</figure>

## 提案手法 — 4 つの設計原則と、その実装

著者らは開発セットに対して **(1) エージェントの振る舞いを手作業で検査する** と **(2) グリッドサーチで構成を選ぶ** の 2 つを回し、そこから 4 つの原則を抽出した。

1. **行動は単純で理解しやすくあるべき** — 多くの bash コマンドは何十ものオプションを持つ。少数のオプションと簡潔なドキュメントのほうがエージェントには使いやすい。
2. **行動はコンパクトかつ効率的であるべき** — 重要な操作は**可能な限り少ない行動へ統合する**。悪い設計とは、高次の操作を実現するのに複数ターンにわたる合成を強いる単純な行動が多数あることである。
3. **環境のフィードバックは情報を与えるが簡潔であるべき**。
4. **ガードレールは誤りの伝播を緩和し回復を早める**。

実装は 4 つの構成要素からなる。

- **検索／ナビゲーション**: `find_file` / `search_file` / `search_dir`。**結果は最大 50 件**で、超えたら結果を出さず「**より具体的なクエリを書け**」と提案する。
- **ファイルビューア**: `open` で**最大 100 行の窓**を開き、`scroll_up` / `scroll_down` / `goto` で動かす。完全なパス・総行数・**窓の前後で省略された行数**・行番号を常に表示する。
- **ファイル編集器**: `edit <n>:<m>` が開始行・終了行・置換テキストの 3 引数を取り、**単一のステップで複数行を置換**する。編集後は**ファイルビューアが自動的に更新後の内容を表示する**（確認のための追加コマンドが要らない）。
- **文脈管理**: 思考と行動を毎ターン生成させ、**直近 5 つより前の観測は 1 行に折り畳む**（`Old output omitted (101 lines)`）。不正な形式の生成にはエラーを返して再試行させ、**受理されたら最初のもの以外の過去のエラーメッセージは履歴から消す**。

<figure>

![](../../raw/assets/2024-swe-agent/fig03-file-viewer-and-edit.png)

<figcaption>図2（再掲, 原典の Figure 3）: ファイルビューアと edit コマンドは統合されている。(a) 開いているファイルを行番号つきで表示し、上下に省略された行数も示す。(b) エージェントが edit 404:407 で範囲を置換する。編集後、ファイルビューアが自動的に更新後の版を示す。</figcaption>
</figure>

**リンタをガードレールとして組み込む**のが効く工夫である。編集を適用したうえで `flake8 --isolated --select=F821,F822,F831,E111,E112,E113,E999,E902` を走らせ、**構文エラーが出たら編集を差し戻す**。差し戻すときエージェントへ返すのは 3 点セット——**どのエラーが出たか・編集後がどう見えたはずか・編集前の元の内容**。著者らは変種を試したうえで「**3 つすべてが要る**」と結論しており、理由が具体的である。誤りの種類がないと誤診する、変更後の断片がないと同じコマンドを再発行しがち、**元の内容がないと数ターン前の（より新しいという理由だけで選ばれた）誤った内容に対して編集を生成してしまう**。

## 実験結果と知見

**主結果。** SWE-bench の完全なテスト集合（2,294 インスタンス、12 リポジトリ）で **12.47%（286/2,294）**、Lite（300）で **18.00%**。従来の最良は非対話的 RAG の **3.79%**（Claude 3 Opus）で、**3 倍以上**。Shell-only ベースライン（11.00%）に対しては **相対 64% 改善**。Claude 3 Opus へ移しても 10.46%（full）で、**特定モデルへの過適合ではない**。HumanEvalFix では Python 87.7 / JS 89.7 / Java 87.9。コストは 1 インスタンスあたり平均 **$1.59**（RAG は $0.13 なので 8〜13 倍高い）、予算上限は $4。

### 最も鋭い結果 — 人間の UI を模した検索が、検索なしより悪い

Table 3 のアブレーション（SWE-bench Lite, GPT-4 Turbo, 既定 18.0%）が本論文の主張の中核である。

| 軸 | 設定 | % Resolved |
| --- | --- | --- |
| **Search** | Summarized（既定） | **18.0** |
| | **Iterative** | **12.0**（↓6.0） |
| | No search | 15.7（↓2.3） |
| **Editor** | `edit` + linting（既定） | **18.0** |
| | `edit` のみ（linting なし） | 15.0（↓3.0） |
| | `edit` なし | 10.3（↓7.7） |
| **File Viewer** | 100 行（既定） | **18.0** |
| | 30 行 | 14.3（↓3.7） |
| | ファイル全体 | 12.7（↓5.3） |
| **Context** | 直近 5 観測（既定） | **18.0** |
| | 全履歴 | 15.0（↓3.0） |
| | 実演なし | 16.3（↓1.7） |

**Iterative search（12.0）が No search（15.7）より悪い**——これが決定的である。Iterative search は `Vim` や VSCode の検索 UI を直接模したもので、**結果を 1 件ずつ `next` / `prev` で送る**。人間には自然だが、エージェントは**全一致を網羅的に舐めようとして `next` を呼び続け**、コスト予算とコンテキストウィンドウを枯渇させる。**「人間にとって良い UI」がエージェントには積極的に有害でありうる**ことの、直接的な実証になっている。

<figure>

![](../../raw/assets/2024-swe-agent/fig05-search-interfaces.png)

<figcaption>図3（再掲, 原典の Figure 5）: 3 つの検索インターフェースの比較。左が No Search（ls + cd の手作業、grep の結果を解釈できない）、中央が Iterative Search（next/prev で 1 件ずつ。結果が尽きるまで繰り返される）、右が Summarized Search（全結果を単一の出力で示し、多すぎれば再試行を促す）。</figcaption>
</figure>

ファイルビューアの窓サイズも**両側に最適点がある**——30 行では足りず（14.3）、ファイル全体では多すぎる（12.7）。「全部見せれば良い」でも「絞れば良い」でもない。

### 振る舞いの分析 — 編集の失敗は連鎖する

- **編集は依然として難所である。** **2,294 インスタンスのうち 1,185（51.7%）が失敗した編集を 1 回以上含む**（軌跡あたり中央値 3 回、最大 33 回）。解決できたインスタンスでは 31.5% に下がる。
- **回復確率は失敗が積むほど落ちる。** 編集のどんな試みも最終的に成功する確率は **90.5%** だが、**1 回失敗した直後には 57.2%** になる。言い換えれば **1 回の編集エラーで 42.8% はもう回復しない**。
- **エージェントは速く成功し、ゆっくり失敗する。** 解決できたものは**中央値 $1.21・12 ステップ**、できなかったものは**平均 $2.52・21 ステップ**。**解決できたインスタンスの 93.0% は予算を使い切る前に提出**している（全体では 69.0%）。著者らの結論は明快で、**予算やトークン上限を上げても性能はさほど伸びないだろう**。
- **失敗の内訳**（GPT-4o による自動分類、$n=248$、著者との一致 87%）: **Incorrect Implementation 39.9% ＋ Overly Specific Implementation 12.1% = 52.0%** が「妥当な場所を触ったが解が正しくない／一般的でない」。**連鎖する編集失敗が 23.4%。**

<figure>

![](../../raw/assets/2024-swe-agent/fig08-failure-modes.png)

<figcaption>図4（再掲, 原典の Figure 8）: 未解決インスタンスの失敗モード分布。Incorrect Implementation 39.9、Failed to Recover from Edit 23.4、Failed to Find Edit Location 12.9、Overly Specific Implementation 12.1、Gave Up Prematurely 4.8、Failed to Find Relevant File 2.4、Can't Reproduce 2.4、Ran Out of Time 2.0。</figcaption>
</figure>

- **位置特定は BM25 より得意。** 編集したファイル集合の F1 は **SWE-agent 59.05% vs BM25 45.47%**。対話的に探せることが効いている。
- **生成されるパッチは gold より大きい**（追加行の中央値 12.0 vs 7.0）。差の大半は**再現コード**に由来する。そして**解決できた編集は解決できなかった編集より小さい**——複数箇所にまたがる修正はエージェントに難しいまま。
- **pass@6 は 32.67%**（pass@1 は 17.94 ± 0.49）。平均のばらつきは小さいが、**インスタンスごとの解決はかなり揺れる**。

## 限界・批判的視点

- **絶対水準は低い。** 12.47% は当時の最高性能だが、**87.5% は解けていない**。「SOTA」という表現に引きずられて能力を過大評価しないこと。
- **2024 年前半の論文である。** ベースは GPT-4 Turbo と Claude 3 Opus。SWE-bench の解決率はその後大きく伸びており（→ [[coding-agents]]）、**数値そのものは陳腐化している**。陳腐化していないのは **ACI という概念と、アブレーションが示した設計の非自明さ**のほうである。
- **ACI の設計過程が手作業である。** 著者ら自身が限界節で認めている——構成要素の多くは軌跡を人が眺めて気づいた繰り返しパターンから作られた。**インスタンステンプレートの「ヒント」も、失敗を人が見て書き足したもので、「我々の手作業のアプローチが確かにスケールしない」と明記**している。この自動化を志向したのが後の [[summaries/2026-meta-harness]] の系譜である。
- **モデル依存が残る。** Llama 3（コンテキスト 8k）や DeepSeek Coder では「エージェント設定における性能が不十分」だったと述べるにとどまり、**どう不十分だったかの数値がない**。ACI が「モデルを問わず効く」ことの証拠は GPT-4 Turbo と Claude 3 Opus の 2 点のみ。
- **実演の効果は自分たちでも疑っている。** アブレーションでは実演なしで 16.3（↓1.7）と小さく、著者らは「**実演がドメイン固有の問題解決の機微を理解する助けになっているかは確信していない。役割は主として整形式のコマンドを発行することを学ぶ助けだろう**」と書く。
- **原典側の不整合。** Table 4 のドキュメント列で **`scroll_down` が "Moves the window up 100 lines"、`scroll_up` が "Moves the window down 100 lines"** と上下が逆になっている。この列は**実際に LM へ提示される文字列**なので、もしそのまま渡されていたなら誤ったドキュメントで運用されていたことになる。また HumanEvalFix の成績が **Abstract で 87.7%、§5 本文で 88.3%** と食い違う。
- **安全性の前提が Docker のみ。** 著者らは「仮想化されたハードウェア隔離ほど安全とは見なされない」と正直に書き、**「コンテナ脱出の脆弱性を意図的に突くよう作られてはいないコード」には十分**という条件つきで許容している。エージェントに任意コードを実行させる系すべてに共通する前提である（→ [[agent-safety-and-guardrails]]）。

## 実装・運用上の示唆

- **ツールは人間の CLI の流用で済ませない。** 「シェルがあるから何でもできる」は行動空間としては真だが、**エージェントの性能としては偽**である。よく使う操作を単一コマンドへ畳む。
- **編集は「範囲指定の置換」＋「即時の再表示」で設計する。** 行番号を引数にすれば算術が消え、編集後に自動で見せれば確認のターンが消える。
- **検証を通らない操作は適用せず、差し戻す。** そのとき返すのは**エラー・変更後・変更前の 3 点セット**。これは編集に限らず、失敗しうる操作すべてに転用できる型である。
- **観測の折り畳みは「構造を残して内容を捨てる」**。履歴の順序と構造は保ち、古い観測の中身だけを `Old output omitted (N lines)` に置き換える。トークンが減るだけでなく、**古くなったファイル内容を見せない**効果が大きい（→ [[context-engineering]]）。
- **不正な形式の応答は履歴から削除する。** 有効な応答が来たら、不正だった行動と応答を履歴から取り除く。**整形式の応答それぞれが in-context の実演になる**ので、この「勢い（momentum）」が後半のターンを支える。
- **検索結果には上限を置き、超えたら結果でなく助言を返す。** 「50 件を超えたらより具体的なクエリを書け」は、エージェントの網羅癖に対する直接の対処。
- **設定をファイルに追い出して ACI を差し替え可能にする。** プロンプトテンプレート・コマンドファイル・制御フロー・環境変数の 4 カテゴリ。**インターフェースを実験対象にするには、まずインターフェースを設定にする必要がある。**

## 用語と略称

- **ACI** = Agent-Computer Interface（エージェント–コンピュータインターフェース）。LM エージェントがコンピュータと相互作用するためのインターフェース層。API（ソフトウェア向け）・UI（人間向け）に対する第 3 の類型
- **LM / LLM** = Language Model / Large Language Model（言語モデル／大規模言語モデル）
- **SWE-bench** = 12 の Python リポジトリの実際の GitHub イシュー 2,294 件から自動収集したベンチマーク。**Lite** は自己完結したバグ修正 300 件の部分集合、**Dev** は 225 件
- **% Resolved / pass@1** = 生成パッチを適用してすべてのテストが通ったインスタンスの割合
- **HumanEvalFix** = バグのあるコードを修正するデバッグのベンチマーク（言語あたり 164 問題）
- **RAG** = Retrieval-Augmented Generation（検索拡張生成）。ここでは BM25 で関連ファイルを引いて非対話的にパッチを生成するベースライン
- **BM25** = 単語の重なりに基づく古典的な検索スコアリング
- **Shell-only** = InterCode 由来の、素の Linux シェルだけを与えるベースライン
- **linter / linting** = ソースコードの静的検査。ここでは `flake8`
- **gold patch** = そのイシューに対して人間の保守者が実際に書いた正解の修正
- **hunk** = パッチにおける連続した変更のかたまり
- **trajectory**（軌跡）= エージェントが辿った思考・行動・観測の系列
- **HCI** = Human-Computer Interaction（人間–コンピュータ相互作用）
- **InterCode** = 対話的コーディングのタスク環境。SWE-agent の環境実装の土台

## 関連ページ

- [[summaries/2023-lost-in-the-middle]] — ファイルビューアの窓サイズに両側の最適点がある理由の背景

- [[summaries/2023-swe-bench]] — SWE-bench の一次資料。同じグループが 7 か月前に作ったベンチマークで、その論文が「エージェントベースのアプローチに期待している」と書いた宿題に本論文が答えている

- [[coding-agents]] — 本ページの主な接続先。ACI はここの中心概念
- [[tool-use-and-function-calling]] — ツールを「人間向け」でなく「エージェント向け」に設計し直すという論点
- [[agent-evaluation]] — SWE-bench とその指標
- [[context-engineering]] — 観測の折り畳み、窓サイズ、気を散らす文脈のコスト
- [[agent-loop]] — 思考・行動・観測のループとその終了条件
- [[agent-safety-and-guardrails]] — 任意コード実行のサンドボックス化
- [[summaries/2022-react]] — 思考と行動を交互に生成する形式の出どころ
- [[summaries/2026-meta-harness]] — 手作業だった ACI 設計の自動化を志向する系譜
- [[summaries/2025-effective-harnesses]] / [[summaries/2026-harness-design]] — 同じ問題を実務側から扱った Anthropic の記録
